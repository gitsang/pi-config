description: Review the current uncommitted diff for issues

Review the current uncommitted changes in this repository:

1. Run `git status` and `git diff` to see what changed (include staged changes too).
2. Read each changed file and look for bugs, regressions, style issues,
   missing edge cases, or problems with tests.
3. Report findings per file with line references where possible, ordered by severity.
4. Do NOT modify anything unless I ask you to — just report.
