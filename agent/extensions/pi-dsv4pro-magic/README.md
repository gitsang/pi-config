# pi-dsv4pro-magic

When the active model matches `deepseek-v4-pro` (case-insensitive, matched against model id or `provider/id`), this extension replaces the system prompt with a fixed prompt that forces English thinking and a `We need...` thought prefix.

## Usage

No commands and no configuration. It activates automatically for matching models.
