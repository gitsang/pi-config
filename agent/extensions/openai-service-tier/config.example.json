{
  "_comment": "Config locations (precedence: later overrides earlier): global ~/.pi/agent/openai-service-tier.json < extension ./config.json (this file's dir) < project <cwd>/.pi/openai-service-tier.json (trusted). Copy this template to any of them. An entry's PRESENCE marks a provider/model as service-tier-capable; model-level entries win over provider-level.",
  "providers": {
    "saigw-openai": {
      "default": "priority",
      "allowed": ["auto", "default", "flex", "priority"]
    }
  },
  "models": {
    "saigw-openai/gpt-5.6-sol": {
      "default": "priority",
      "allowed": ["priority", "flex"]
    },
    "saigw-openai/gpt-5.6-terra": {
      "default": "priority",
      "allowed": ["priority", "flex"]
    }
  }
}
