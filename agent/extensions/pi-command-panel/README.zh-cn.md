# pi-command-panel

pi 的模糊搜索命令面板。

- 在主编辑器按 `Ctrl+P` 打开面板。
- 输入框第一个字符为 `/` 时也会打开。
- 按名称和描述模糊过滤；`↑`/`↓` 选择，`Tab` 补全，`Enter` 执行，`Esc` 取消。

## 使用

- 按 `Ctrl+P`，或在输入框开头输入 `/`。
- `/panel` 也可直接打开面板。

无需配置。
